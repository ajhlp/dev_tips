const DEFAULT_PREFIX = '_FPDragon_';
// initial state
const state = {
    beforeLoginURLKey: DEFAULT_PREFIX + 'beforeLoginURL',
    userTokenKey: DEFAULT_PREFIX + 'token',
    opsKey: DEFAULT_PREFIX + 'ops',
}

// getters
const getters = {
    
}

// actions
const actions = {
    
}

// mutations
const mutations = {
    
}
  
export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
